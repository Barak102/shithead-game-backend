import { ShitheadGame } from 'src/shared/entities/shit-head-game';

export const putCard = ({ context }: any, params: any): ShitheadGame => {
  //const {players} = context;
  //players[0].deck.find()
  return { ...context };
};
